import React, {useState} from 'react'

/* const addToCart = (item) => {
    // const [cartItem] = useState([item])
    sessionStorage.setItem('cart', JSON.stringify(cartItem))
}

export default {addToCart} */